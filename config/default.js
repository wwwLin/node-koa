const config = {

    //启动端口8000
    port: 8000,

    //数据库配置
    database: {
        DATABASE: "lin_node",
        USERNAME: "root",
        PASSWORD: "root",
        PORT: "3306",
        HOST: "localhost"
    }
}

module.exports = config;